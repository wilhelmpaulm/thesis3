<?php

class Form_casesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_cases')->truncate();

		$form_cases = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_cases')->insert($form_cases);
	}

}
