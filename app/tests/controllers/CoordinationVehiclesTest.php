<?php

use Mockery as m;
use Way\Tests\Factory;

class CoordinationVehiclesTest extends TestCase {

	public function __construct()
	{
		$this->mock = m::mock('Eloquent', 'Coordination_vehicle');
		$this->collection = m::mock('Illuminate\Database\Eloquent\Collection')->shouldDeferMissing();
	}

	public function setUp()
	{
		parent::setUp();

		$this->attributes = Factory::coordination_vehicle(['id' => 1]);
		$this->app->instance('Coordination_vehicle', $this->mock);
	}

	public function tearDown()
	{
		m::close();
	}

	public function testIndex()
	{
		$this->mock->shouldReceive('all')->once()->andReturn($this->collection);
		$this->call('GET', 'coordination_vehicles');

		$this->assertViewHas('coordination_vehicles');
	}

	public function testCreate()
	{
		$this->call('GET', 'coordination_vehicles/create');

		$this->assertResponseOk();
	}

	public function testStore()
	{
		$this->mock->shouldReceive('create')->once();
		$this->validate(true);
		$this->call('POST', 'coordination_vehicles');

		$this->assertRedirectedToRoute('coordination_vehicles.index');
	}

	public function testStoreFails()
	{
		$this->mock->shouldReceive('create')->once();
		$this->validate(false);
		$this->call('POST', 'coordination_vehicles');

		$this->assertRedirectedToRoute('coordination_vehicles.create');
		$this->assertSessionHasErrors();
		$this->assertSessionHas('message');
	}

	public function testShow()
	{
		$this->mock->shouldReceive('findOrFail')
				   ->with(1)
				   ->once()
				   ->andReturn($this->attributes);

		$this->call('GET', 'coordination_vehicles/1');

		$this->assertViewHas('coordination_vehicle');
	}

	public function testEdit()
	{
		$this->collection->id = 1;
		$this->mock->shouldReceive('find')
				   ->with(1)
				   ->once()
				   ->andReturn($this->collection);

		$this->call('GET', 'coordination_vehicles/1/edit');

		$this->assertViewHas('coordination_vehicle');
	}

	public function testUpdate()
	{
		$this->mock->shouldReceive('find')
				   ->with(1)
				   ->andReturn(m::mock(['update' => true]));

		$this->validate(true);
		$this->call('PATCH', 'coordination_vehicles/1');

		$this->assertRedirectedTo('coordination_vehicles/1');
	}

	public function testUpdateFails()
	{
		$this->mock->shouldReceive('find')->with(1)->andReturn(m::mock(['update' => true]));
		$this->validate(false);
		$this->call('PATCH', 'coordination_vehicles/1');

		$this->assertRedirectedTo('coordination_vehicles/1/edit');
		$this->assertSessionHasErrors();
		$this->assertSessionHas('message');
	}

	public function testDestroy()
	{
		$this->mock->shouldReceive('find')->with(1)->andReturn(m::mock(['delete' => true]));

		$this->call('DELETE', 'coordination_vehicles/1');
	}

	protected function validate($bool)
	{
		Validator::shouldReceive('make')
				->once()
				->andReturn(m::mock(['passes' => $bool]));
	}
}
