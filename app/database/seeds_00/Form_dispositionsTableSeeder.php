<?php

class Form_dispositionsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('form_dispositions')->truncate();

		$form_dispositions = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('form_dispositions')->insert($form_dispositions);
	}

}
