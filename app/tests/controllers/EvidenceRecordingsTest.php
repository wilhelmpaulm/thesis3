<?php

use Mockery as m;
use Way\Tests\Factory;

class EvidenceRecordingsTest extends TestCase {

	public function __construct()
	{
		$this->mock = m::mock('Eloquent', 'Evidence_recording');
		$this->collection = m::mock('Illuminate\Database\Eloquent\Collection')->shouldDeferMissing();
	}

	public function setUp()
	{
		parent::setUp();

		$this->attributes = Factory::evidence_recording(['id' => 1]);
		$this->app->instance('Evidence_recording', $this->mock);
	}

	public function tearDown()
	{
		m::close();
	}

	public function testIndex()
	{
		$this->mock->shouldReceive('all')->once()->andReturn($this->collection);
		$this->call('GET', 'evidence_recordings');

		$this->assertViewHas('evidence_recordings');
	}

	public function testCreate()
	{
		$this->call('GET', 'evidence_recordings/create');

		$this->assertResponseOk();
	}

	public function testStore()
	{
		$this->mock->shouldReceive('create')->once();
		$this->validate(true);
		$this->call('POST', 'evidence_recordings');

		$this->assertRedirectedToRoute('evidence_recordings.index');
	}

	public function testStoreFails()
	{
		$this->mock->shouldReceive('create')->once();
		$this->validate(false);
		$this->call('POST', 'evidence_recordings');

		$this->assertRedirectedToRoute('evidence_recordings.create');
		$this->assertSessionHasErrors();
		$this->assertSessionHas('message');
	}

	public function testShow()
	{
		$this->mock->shouldReceive('findOrFail')
				   ->with(1)
				   ->once()
				   ->andReturn($this->attributes);

		$this->call('GET', 'evidence_recordings/1');

		$this->assertViewHas('evidence_recording');
	}

	public function testEdit()
	{
		$this->collection->id = 1;
		$this->mock->shouldReceive('find')
				   ->with(1)
				   ->once()
				   ->andReturn($this->collection);

		$this->call('GET', 'evidence_recordings/1/edit');

		$this->assertViewHas('evidence_recording');
	}

	public function testUpdate()
	{
		$this->mock->shouldReceive('find')
				   ->with(1)
				   ->andReturn(m::mock(['update' => true]));

		$this->validate(true);
		$this->call('PATCH', 'evidence_recordings/1');

		$this->assertRedirectedTo('evidence_recordings/1');
	}

	public function testUpdateFails()
	{
		$this->mock->shouldReceive('find')->with(1)->andReturn(m::mock(['update' => true]));
		$this->validate(false);
		$this->call('PATCH', 'evidence_recordings/1');

		$this->assertRedirectedTo('evidence_recordings/1/edit');
		$this->assertSessionHasErrors();
		$this->assertSessionHas('message');
	}

	public function testDestroy()
	{
		$this->mock->shouldReceive('find')->with(1)->andReturn(m::mock(['delete' => true]));

		$this->call('DELETE', 'evidence_recordings/1');
	}

	protected function validate($bool)
	{
		Validator::shouldReceive('make')
				->once()
				->andReturn(m::mock(['passes' => $bool]));
	}
}
